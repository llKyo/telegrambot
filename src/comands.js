const start = require('./commands/start');
const settings = require('./commands/settings');
const help = require('./commands/help');
const sismos = require('./commands/sismos');
const text = require('./commands/text');


module.exports = {
    start,
    settings,
    help,
    sismos,
    text
};